{application, fabric, [
    {description, "Routing and proxying layer for CouchDB cluster"},
    {vsn, git},
    {registered, []},
    {applications, [kernel, stdlib, config, couch, couch_stats, rexi, mem3, twig]}
]}.
